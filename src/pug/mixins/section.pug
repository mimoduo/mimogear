//- ================
//- Section Mixin
//- ================

mixin section(modifier)
  if modifier
    .section(class="section-" + modifier)
      .compartment
        block
  else
    .section
      .compartment
        block
